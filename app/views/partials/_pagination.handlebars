{{#if (validPages pages)}}
<nav class="mt-2">
    <ul class="pagination">
        {{#for 1 pages 1}}
        <li class="page-item"><a class="page-link" href="?page={{this}}">{{this}}</a></li>
        {{/for}}
    </ul>
</nav>
{{/if}}